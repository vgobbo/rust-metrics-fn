[package]
name = "metrics-fn-example-prometheus"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = { version = "0.8" }
rocket = { version  = "0.5.0-rc.1" }
metrics-fn = { path = "../metrics-fn", features = [ "prometheus" ] }
