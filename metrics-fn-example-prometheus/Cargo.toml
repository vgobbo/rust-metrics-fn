[package]
name = "metrics-fn-example-prometheus"
version = "0.1.0"
edition = "2021"

[dependencies]
metrics-fn = { path = "../metrics-fn", features = [ "prometheus", "log" ] }
prometheus = { version = "0.13" }
rand = { version = "0.8" }
rocket = { version  = "0.5.0-rc.1" }
